const origin = Vector3.new()
const pos = Vector3.new(1, 2, 3)
const identity = CFrame.new()
const lookingAt = CFrame.new(pos, origin)
const fromNumbers = CFrame.new(1, 2, 3)
const brick = BrickColor.new("Really red")
const size = UDim2.new(0.5, 0, 0.5, 0)