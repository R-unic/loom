const Loom = require("@game/ReplicatedStorage/include/loom_runtime")
type ReturnType<T> = unknown
type ElementOf<T> = unknown
type Exclude<T, U> = unknown
type Awaited<T> = unknown
type AsMut<T> = { [keyof<T>]: index<T, keyof<T>> }
type Point = {
  read x: number,
  read y: number,
}
const width: number = 3
const letter: string = "a"
const key: "x" | "z" = "x"
const awaited: number = 7
print(width, letter, key, awaited, point.x)