declare interface Shape { x: number; y: number; }
declare interface ShapeStatic {
    create: fn(): Shape;
    create: fn(x: number, y: number): Shape;
}
declare let Shape: ShapeStatic;

let a = Shape.create();
let b = Shape.create(1, 2);
