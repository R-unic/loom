[serializable]
interface Player {
    id: u8;
    name: string;
}

fn _trigger_usage(): void {
    let payload = serialize_binary(none as never as Player);
    let restored = deserialize_binary::<Player>(payload);
}
